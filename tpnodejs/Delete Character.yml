info:
  name: Delete Character
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/api/characters/:id
  params:
    - name: id
      value: "26"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
