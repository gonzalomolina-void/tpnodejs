info:
  name: Get Character
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/api/characters/:id
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
