info:
  name: Update Character
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3000/api/characters/:id
  params:
    - name: id
      value: "26"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Darth Vader",
        "race": "Human-Machine",
        "role": "Dark Lord",
        "level": 90,
        "universe": "Star Wars"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
